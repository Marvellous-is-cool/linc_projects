<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Select Interest</title>
    <!-- Google Fonts -->
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap"
      rel="stylesheet"
    />
    <!-- FontAwesome -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    />
    <!-- Animate.css -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"
    />
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <%- include('header') %>
    <div class="container animate__animated animate__fadeIn">
      <h1 class="title">
        Hello <span><%= name %></span>! Select your area of interest:
      </h1>

      <form
        action="/branches"
        method="post"
        class="form animate__animated animate__fadeInUp"
      >
        <input type="hidden" name="name" value="<%= name %>" />

        <div class="form-group">
          <label class="radio-label">
            <input type="radio" name="interest" value="linguistics" required />
            <span class="radio-text">Linguistics</span>
          </label>
        </div>

        <div class="form-group">
          <label class="radio-label">
            <input
              type="radio"
              name="interest"
              value="communication"
              required
            />
            <span class="radio-text">Communication</span>
          </label>
        </div>

        <div class="form-group">
          <label class="radio-label">
            <input type="radio" name="interest" value="both" required />
            <span class="radio-text">Both</span>
          </label>
        </div>

        <button type="submit" class="btn-submit">
          Next <i class="fas fa-arrow-right"></i>
        </button>
      </form>
    </div>
    <%- include('footer') %>

    <style>
      body {
        font-family: "Roboto", sans-serif;
        background: linear-gradient(135deg, #1c1c1c, #343434);
        color: #fff;
        display: flex;
        justify-content: center;
        align-items: center;
      }

      .container {
        text-align: center;
        width: 100%;
        max-width: 400px;
        padding: 20px;
        background-color: rgba(255, 255, 255, 0.1);
        border-radius: 10px;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
      }

      .title {
        font-size: 2rem;
        margin-bottom: 20px;
        line-height: 1.2;
      }

      .title span {
        color: #00d9ff;
      }

      .form-group {
        margin-bottom: 15px;
        text-align: left;
      }

      .radio-label {
        display: flex;
        align-items: center;
        font-size: 1.1rem;
      }

      .radio-label input[type="radio"] {
        margin-right: 10px;
        accent-color: #00d9ff;
        cursor: pointer;
      }

      .radio-text {
        color: #ccc;
        font-weight: 500;
      }

      .btn-submit {
        display: inline-block;
        background-color: #00d9ff;
        color: #1c1c1c;
        padding: 10px 20px;
        font-size: 1rem;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
      }

      .btn-submit:hover {
        background-color: #00b3e3;
      }

      .btn-submit i {
        margin-left: 10px;
      }
    </style>
  </body>
</html>
